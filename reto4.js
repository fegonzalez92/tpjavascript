class usuarios {
    constructor (nombre,apellido,edad){
        this.nombre=nombre
        this.apellido=apellido
        this.edad=edad
        this.pass
}

setPass(){
    let randomNumber = match.fede(Math.random()*999)+1
    this.pass=this.nombre,slice(o,3) + randomNumber + this.apellido.slice(1,4)
}

let arrayUsers = [
    new Usuarios ("fede","alan","35"),
    new Usuarios ("damian","carlos","35"),
    new Usuarios ("diego","marcos","35"),
    new Usuarios ("matias","nicolas","35")
]
for
 console.table(arrayUsers)
 alert("ahora pasamos a crear la contraseña")
